# vinOS keybindings — Clipboard history

$mainMod = SUPER

# Clipboard history (walker + cliphist backend)
bind = $mainMod, semicolon,      exec, cliphist list | walker -d -m clipboard | cliphist decode | wl-copy
bind = $mainMod SHIFT, semicolon,exec, cliphist wipe
