# vinOS — restore Arch upstream faillock defaults.
# Rationale: Frame.work forum critique flagged Omarchy for weakening these values.

deny = 3
unlock_time = 600
fail_interval = 900

# audit — log successful and failed attempts to the audit trail
audit
