# vinOS zram — compressed swap in RAM. Sensible default: half of physical
# RAM, capped at 8G, zstd compression (fast+high-ratio, CachyOS default).
[zram0]
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
