---
title: "Reference"
description: "Where things live, environment variables the vinos-* family reads, and the full CLI --help for every shipped command."
weight: 70
---

Three tables you'll want on hand:

- **[File layout](/docs/reference/file-layout/)** — every path vinOS
  writes, reads, or expects.
- **[Environment variables](/docs/reference/environment-vars/)** —
  every `VINOS_*` var, its default, and what changes when you set it.
- **[CLI reference](/docs/reference/cli-reference/)** — full `--help`
  output for every `vinos-*` command shipped in the ISO.
