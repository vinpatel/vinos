# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(vinos-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, vinos-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(vinos-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, vinos-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, vinos-launch-browser --private
bindd = SUPER SHIFT, N, Editor, exec, vinos-launch-editor

# Add extra bindings
# bindd = SUPER SHIFT, A, ChatGPT, exec, vinos-launch-webapp "https://chatgpt.com"
# bindd = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Overwrite existing bindings, like putting vinOS Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, vinOS menu, exec, vinos-menu
