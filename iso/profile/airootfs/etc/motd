Welcome to [38;2;23;147;209mvinOS Live[0m — an opinionated Arch layer by Vin Patel.

For Wi-Fi, authenticate to the wireless network using the [35miwctl[0m utility.
For mobile broadband (WWAN) modems, connect with the [35mmmcli[0m utility.
Ethernet, WLAN and WWAN interfaces using DHCP should work automatically.

Install to disk with [35msudo iso/flash.sh[0m onto a USB, or run
[35mgit clone && bash install.sh[0m to convert this live session into a
permanent vinOS install.

