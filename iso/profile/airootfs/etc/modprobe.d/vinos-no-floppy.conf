# QEMU's default machine still emulates an ISA floppy controller, and the
# kernel probe on `dev fd0, sector 0` produces a visible I/O error at boot
# with no functional purpose. Modern target hardware has no floppy either.
# Blacklist keeps journalctl -b -p err clean on both.
blacklist floppy
install floppy /bin/true
