#!/bin/bash

# vinos:summary=Launch a TUI command in the default terminal with vinOS styling
# vinos:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.vinos."$(basename "$1")" -e "$1" "${@:2}"
