#!/usr/bin/env bash
# vinos-theme-pick — walker theme picker.
set -euo pipefail
command -v walker >/dev/null || { vinos-theme-list; exit 0; }
pick=$(vinos-theme-list | sed 's/^[● ] //' | walker -d -m dmenu -p "Pick a theme…" 2>/dev/null)
[[ -n "$pick" ]] && exec vinos-theme-set "$pick"
