#!/usr/bin/env bash
# vinos-lock — lock the screen.
set -euo pipefail
if command -v hyprlock >/dev/null; then pidof hyprlock >/dev/null || exec hyprlock
elif command -v swaylock >/dev/null; then exec swaylock -f
else exec loginctl lock-session
fi
