#!/usr/bin/env bash
# vinos-shell — send an IPC call to the running desktop shell.
# vinOS-branded passthrough to omarchy-shell. See NOTICES.md for the
# heritage.
set -euo pipefail
exec omarchy-shell "$@"
