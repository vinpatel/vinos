#!/usr/bin/env bash
# vinos-toggle-topbar — toggle waybar.
set -euo pipefail
if pidof waybar >/dev/null; then
  pkill waybar
  command -v notify-send >/dev/null && notify-send -a vinos "vinOS" "Top bar hidden"
else
  waybar >/dev/null 2>&1 & disown
fi
