#!/usr/bin/env bash
# 02-desktop.sh — Hyprland module (Rule 1: ONLY graphical script). Filled in M5.
set -euo pipefail
# shellcheck source=../lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"
log "02-desktop: skeleton stub (M1) — Hyprland setup lands in M5"
