#!/usr/bin/env bash
# 01-base.sh — core packages + AUR helper. Headless. Filled in M2.
set -euo pipefail
# shellcheck source=../lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"
log "01-base: skeleton stub (M1) — real work lands in M2"
